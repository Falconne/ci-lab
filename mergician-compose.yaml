name: mergician

services:
  mergician:
    build:
      context: .
      dockerfile: Dockerfile.mergician-prod
    restart: unless-stopped
    ports:
      - "5000:5000"
      - "3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      Mergician__GitLab__Url: "${GITLAB_URL:-http://localhost:8081}"
      Mergician__GitLab__InternalUrl: "${GITLAB_INTERNAL_URL:-http://gitlab:8081}"
      Mergician__GitLab__OAuth__ClientId: "${MERGICIAN_GITLAB_OAUTH_CLIENT_ID:-}"
      Mergician__GitLab__OAuth__ClientSecret: "${MERGICIAN_GITLAB_OAUTH_CLIENT_SECRET:-}"
    env_file:
      - path: .env
        required: false
    networks:
      - default
      - ci-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

networks:
  ci-network:
    # Note: We must connect to the ci-lab network and use its internal hostnames to access Gitlab and TeamCity rather than
    # using network mode "host", because that network mode causes complications with port forwarding in WSL2 where we sometimes
    # need to run and debug.
    external: true
    name: ci-lab_ci-network
