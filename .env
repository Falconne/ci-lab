# Repo-level environment file for CI lab
# Adjust tokens and URLs before running `docker compose up` or the bootstrap script.
GITLAB_URL=http://gitlab
TEAMCITY_URL=http://teamcity:8111
GITLAB_TOKEN=CHANGEME_REPLACE_WITH_REAL_TOKEN
TEAMCITY_TOKEN=CHANGEME_REPLACE_WITH_REAL_TOKEN
