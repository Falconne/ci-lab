Perform the following tasks:

1)
The Project Setup Service creates 3 test users for Gitlab. Make it so that those test users are also added as Developers to the group where the test repos were created, so they can push code, create, approve and merge MRs in those test repos.

2)
Change the "landing page" of the Mergician app to show the logged in user's Gitlab activity stream for the last week. To fetch this data, we want to use Gitlab as an oauth provider.

Update the bootstrappers project setup service to set up what is needed on the Gitlab admin side to allow Mergician to be an application that can use Gitlab for oauth.

When a user browses to Mergician, if they are not logged in, they should be redirected so they can login via Gitlab oauth. There should be a logout button on the top right, but otherwise the user should remain logged in after this. If they revisit Mergician in a few days they should remain logged in, so use cookies or do the best practice to make this happen.

Do the changes, then run the bootstrapper and then run Mergician to test that everything loads and fix any issues seen. 

Create a new console project in the VS solution called IntegrationTest. In here, use Playwright to navigate to the Mergician home page and login as one of the test users created by the Project Setup Service. Use the same technique as the Bootstrap project to capture briwser screenshots in /data/screenshots to debug test failures. Once the login functionality is validated, add more tests to this IntegrationTest project to perform some git operations as that test user, then check that their activity is shown in Mergician.

If the tests fail, investigate and continue fixing until the integration test succeeds.

3)
Create a `docs` folder at the top level of the repo and add a `Authentication.md` file there describing how the authentication works. Be concise and focus on summarising with the intent that future runs of copilot agent can read this document to understand the basics of the auth system without having to read the entire codebase.

Similarly, Add and "Integration Testing.md" explaining how to work with the new IntegrationTest project.