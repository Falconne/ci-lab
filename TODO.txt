In ProjectSetupService, add a step to generate an access token for the b.builder account, with full permissions.Write this token into the .env file as "GITLAB_SERVICE_TOKEN" and add a property in Mergician's app settings to pass this in with. Update the Mergician docker compose file to pass this in like the other existing settings. This will be used by Mergician for background monitoring and for performing merge actions. Update the Readme's configuration section to mention this for production setup.

In Mergician, implement the following tasks. When doing the refactoring tasks, ensure you update all references and ensure the build is still passing.

Move `MergicianSettings` into Entities.

Under Services, create a new folder called Gitlab and move the `GitLabOAuthService.cs` file there. Put all future Gitlab related services here and create folders under Services to group service classes by feature in the future.

`GetValidAccessToken` is duplicated. Move this to a shared service called `GitlabCurrentUser` (under the new Gitlab service folder) based on interface `IGitlabAccessUser` which has a public method `GetValidAccessToken`.

Create another class based off IGitlabAccessUser called `GitlabServiceUser`. This one's implementation of `GetValidAccessToken` should return the Gitlab service token discussed earlier.

Move the `GetProject`, `GetUserEvents` and `GetCurrentUser` methods out of the Oauth service (which doesn't make sense for them) and put them in a new `GitlabService` class. Put future generic Gitlab API calls in here. In this new class, change the methods that take in a `string accessToken` as the first parameter to take in a `IGitlabAccessUser` instead and use GetValidAccessToken() internally.

After these changes, run the CI Lab, bootstrapping and Mergician from a clean start and ensure the integration tests still pass. Do not move on till everything is working again.

Give the landing page of Mergician (once logged in) the tile "Dashboard". Remove the current activity stream. We will replace this with a table of date. First fetch the user's push activity for the last 2 weeks to see what repos and branches they have pushed to and generate a list of branch and repo tuples where the branch still exists in that repo (add a method to `GitlabService` to do this). Also add a `IsPossibleDefaultBranch` method to GitlabService that return true if the branch name is one of `develop`, `main`, `master`. Ignore any push events to possible default branches too.

As mentioned, create a table to replace the activity stream. Style it elegant but simple. See the attached image for an example of hwo I want the data displayed (but make it more elegant and clearer than this mock up). As shown, the entries in the table should be grouped by branch name. For each branch, check if there is a Merge Request (MR) and indicate so in that column (but instead of printing 'Yes', show an appropriate Material Design icon that stands for "Merge Request" or "Pull Request"). Clicking this icon should take use to the Merge Request in Gitlab (in a new tab). If a Merge Request does not exist, use a solid blue "plus" icon that will take you to create that MR from that branch in that repo (in a new tab). If an MR does exist, show the approval status as shown (and include a solid green tick if all approvals are given).

Update ProjectSetupService to create more initial activity as all the tests users so we have something to see in this table when testing. Ensure at least a few cases where the same branch in created in multiple repos so we will see various combinations of states in this table, for each user. Also create MRs for some branches but not others, and in some MRs use one of the other test users to give approval while other MRs are left unapproved so we will see these changes differences too.

Update the Integration tests to remove the check for Activity Stream and add new tests to check for this new dashboard table instead. Make it login and check each user's dashbpard and verify that it comes up the way that is expected based on what was set up with ProjectSetupService.

Spin up a clean CI Lab environment with bootstrapping, run Mergician and run the integration tests and fix any failures found.

At the end, perform a code review and do any clean up and rework of the changes if needed to organise the code better and rationalise duplciate code etc. If any significant changes were made rerun the tests to ensure things still work.