Task 1)
When showing approvals for an MR, just show how many approvals are still needed. This accounts for the problem that some approvals may not actually count if the approver does not have the right permissions. 

The MR should be considered Blocked if this number is greater than 0.

Task 2)
When checking if an MR is blocked, in addition to our current checks, see if there is a Gitlan API call to definitely determine if the MR is blocked. If our checks say it's not blocked, but the API says it is, then mark the MR as Blocked with the reason "Blocked for unknown reason". 