In `GitlabServiceUser`, if `_settings.GitLab.ServiceToken` is IsNullOrWhiteSpace, log an error but also return an error condition to the UI. The application should not be used without this set so it's ok to just display an error in the browser.

Give the landing page of Mergician (once logged in) the tile "Dashboard". Remove the current activity stream. We will replace this with a table of date. First fetch the user's push activity for the last 2 weeks to see what repos and branches they have pushed to and generate a list of records with each record containing the branch name, the corresponding project id and the project name. Filter this list to ones where the branch still exists in that repo (add a method to `GitlabService` to do this). Also add a `IsPossibleDefaultBranch` method to GitlabService that returns true if the branch name is called `develop`, `main` or `master`. Ignore any push events to possible default branches too. The ActivityController.GetActivity method currently returns an anonymous type that includes more data than we need. Rename this method to `GetPushActivity` and use it to provide the dicussed data and make sure to return a record type with just the fields we need.

As mentioned, create a table to replace the activity stream. Style it elegant but simple. See the attached image for an example of how I want the data displayed (but make it more elegant and clearer than this mock up). As shown, the entries in the table should be grouped by branch name. For each branch, check if there is a Merge Request (MR) and indicate so in that column (use an appropriate method to display a nice looking tick). If an MR does exist, show the approval status as shown (and include a solid green tick if all approvals are given).

Update ProjectSetupService to create more initial activity for all the tests users, so we have something to see in this table when testing. Ensure at least a few cases where the same branch in created in multiple repos so we will see various combinations of states in this table, for each user. Also create MRs for some branches but not others, and in some MRs use one of the other test users to give approval while other MRs are left unapproved so we will see these changes differences too. The generated activity must be the same on each new run, so that we can write integration tests to check for known activity.

Update the Integration tests to remove the check for Activity Stream and add new tests to check for this new dashboard table instead. Make it login and check each user's dashbpard and verify that it comes up the way that is expected based on what was set up with ProjectSetupService.

Spin up a clean CI Lab environment with bootstrapping, run Mergician and run the integration tests and fix any failures found.

At the end, perform a code review and do any clean up and rework of the changes if needed to organise the code better and rationalise duplciate code etc. If any significant changes were made rerun the tests to ensure things still work.