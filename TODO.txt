Make the project setup bootstrapper, when creating the TeamCity CIs, set them all to use a VCS trigger so they build when a commit is pushed to any branch. Set the global VCS monitoring quiet period in TeamCity to 4 seconds (or the minimum if this is too low). Make sure the VCS root definitions are set to monitor all branches. Also check that Gitlab Commit Status Publishing is set up for every CI. Check that the Gitlab URL used in the commit status publishing is correct for the services inside the docker compose network. Run the ci lab and bootstrapper to check that all of this works.
--

The dashboard currently waits for all user activity data to be collected before rendering the table. Make it so that the table starts filling in as soon as each activity line is read from the feed and data about merge requests (MRs) are discovered, so the UI updates dynamically. This means the front end needs to be dynamic: