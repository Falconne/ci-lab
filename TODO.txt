Task 1)
Remove the "Play" symbol next to the "next up" Merge Group (MG) in the merge queue.

Task 2)
When showing a "Running" build job, use the "Play" icon instead of the current one.

Task 3)
Reduce the minimum poll cycling time for the merge queue to 30 seconds. That is, after iterating through every MG in the all the queues, before starting the next round of checks, make sure at least 30 seconds have passed since the last round started.

Task 4)
Rename "Add Existing Merge Request..." button to "Add Another MR to Group..."

Task 5)
We only care about the status of branches and MGs when they are set to auto merge. Remove the status chip for MGs that aren't set to auto merge and use a neutral colour for them.

Task 6)
In all MG card displays, if the card is showing an MR title as its main title and the branch as a subtitle, put the status chip for the MG to the right of the branch subtitle. Make _all_ status chips about ~20% smaller than they are now.

Task 7)
Add a new mode for displaying MGs in the dashboard and merge queue views called "Grid View". Add two icon-buttons along the top right for switching between "Card View" (what we currently have) and "Grid View". Make "Grid View" the default.

In Grid view, instead of displaying MGs as cards, use a simple but elegant table layout, where each row contains all the information from an MG. Each row can be subdivided into smaller rows (with lighter borders) for divding the info for each branch into. The common info for a MG should be shown in cells that span the whole height.

All the build jobs can be displayed the same way they are now, but in one cell.

In the dashboard page, the tables should be split up over the existing sections, such as "Today", "Last Week", etc.

Task 8)
For now, the MG Details page can keep the cards view. But change the Untrack button to the same level as the "Back to Dashboard" link (but aligned right). Also move the status chip for the MG to the same level and to the left of the Untrack button.

Also, change the main title on the MG Details page to be ~20% smaller than it is now.