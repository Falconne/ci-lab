Create a `scripts/integration-test.sh` similar to the bootstrap.sh that will try to run the IntegrationTest.csproj project natively if possible otherwiser build a container and run it. Create a Dockerfile for this in the root similar to the bootstrap Dockerfile. Move the reusable code in bootstrap.sh to a common.sh file so that the new script can use it as well.

When branch activity events are returned from the backend, include a new field "LastUpdatedTime" which is the time the last push activity to the branch occured. Show this in the UI in a "Last Updated" column. Make it display in friendly format (e.g. "14 seconds ago", "2 hours ago", "3 days ago", etc.) updated once a minute. Howvering over the value should show the exact date and time in local timezone.