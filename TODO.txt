Task 1)
Move the "Add Merge Request..." button to the bottom of the page, below the branch cards. Give it a tooltip: "Manually add a merge request to this group".

Task 2)
In the dashboard, there should be a larger gap between the "Today" title and the filter text box.

Task 3)
The Merge Group cards in the dashboard should be double the width they currently are.

Task 4)
The "Filter by branch" text box should be the width of one merge group card (after the double width change).