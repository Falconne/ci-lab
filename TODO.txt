It seems that even though the Gitlab pipeline publishing should be set up in the CI Lab environment from TeamCity to Gitlab, the pipelines are not appearing as external pipelines in the MRs created by the Project setup service. This may be due to the differences in service names inside the docker compose network the two services are in. Check for any possible config issues and fix. Run the lab and bootstrapper and inspect the MRs after the TeamCity builds are done to see if they appear.
--

The dashboard currently waits for all user activity data to be collected before rendering the table. Make it so that the table starts filling in as soon as each activity line is read from the feed and data about merge requests (MRs) are discovered, so the UI updates dynamically. This means the front end needs to be dynamic: